# Reference only. These are not working credentials.
# Future collectors will explicitly load the local atlas-credentials.env file.
# Never expose these values in frontend code, generated reports, or logs.
MONGODB_USERNAME=replace_with_database_username
MONGODB_PASSWORD=replace_with_database_password
MONGODB_URI=replace_with_atlas_connection_string
